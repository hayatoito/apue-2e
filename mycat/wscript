def build(bld):

  progs = 'mycat fgetsfputs getcputc'

  for prog in progs.split():
    bld(features = 'cc cprogram',
        source = prog + '.c',
        target = prog,
        uselib_local = ['apue',],
        )
